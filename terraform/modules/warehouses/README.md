# Snowflake Warehouse Module

Recommended module inputs:

- warehouse name
- size
- min/max cluster count
- auto-suspend seconds
- statement timeout
- workload owner role

All warehouses should start suspended, auto-resume, and be isolated by workload.
